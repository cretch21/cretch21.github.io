# cretch21.github.io

Static GitHub Pages site that serves `.well-known/apple-app-site-association` for the BookApp / Matilda Books iOS app's Universal Links.

Any path under `/bookapp/share*` is claimed by the app — visiting it from iOS opens BookApp instead of the browser.
